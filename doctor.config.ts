export default {
  // pdfjs-dist powers PDF extraction/rendering and 6.1.200 is the current npm release.
  // React Doctor reports its Socket supply-chain score as 31/100; keep the
  // check enabled while accepting that reviewed floor for this prototype.
  supplyChain: {
    minScore: 31,
  },
}
